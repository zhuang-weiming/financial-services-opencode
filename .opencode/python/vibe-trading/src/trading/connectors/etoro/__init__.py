"""eToro Public API connector (demo + real read/write).

Demo and real share one API key pair; profiles select ``/demo`` vs production
API paths. Live write profiles require mandate enforcement via the live action
gate.
"""
