# raw-log 2026-08-11

## 14:00 SELL_LADDER 日线数据集中化迁移 (DATA)

- **触发**: 用户指出 `sell-ladder/data/` 89 个 CSV 存放分散、难以复用（"都删掉?"）
- **决策**: 迁移集中化到仓库根 `data/market/daily/<code>_<name>.csv`（统一命名 + INDEX.md），跨 subagent 共享
- **执行**:
  1. `migrate_data.py` 迁移 89/89 文件成功（79 raw_daily + 4 CDMO + 6 tech-pool），名称经 Sina 批量获取并清洗空格
  2. 删除遗留 `wt_daily_300725*.csv`（WT 现全部现场计算）
  3. 新建 `data_loader.py`：load_daily/get_name/list_universe/_sina_download；搜索序 = 集中目录 → 旧位置 fallback → Sina 下载落盘
  4. `sell_ladder.py`/`backtest_v090.py`/`backtest_seed_2026.py` 加载逻辑全部委托 data_loader（签名兼容）
  5. 旧 `data/` 源文件经 md5 逐字节验证后删除（仅保留 `data/mcp/` 证据链目录）
  6. SKILL.md / `data/market/README.md` 数据路径描述已更新
- **验证**:
  - `sell_ladder.py --ticker 300725` 端到端（含 CDMO peers + 板块 ETF，走集中目录）✅
  - `backtest_v090.py --variant v2.5 --weekday 4` 全 20 标的与 BT-009-v25 基线**完全一致**（nav 逐 ticker diff = 0.00e+00，n_eval 匹配）✅
- **状态标签**: REINFORCED
- **经验**: 仓库根解析需用 `Path(__file__).resolve().parents[4]`（文件级），dir 级会差一级；改路径后清 `__pycache__`
- **遗留**: `.opencode/skills/alpha-engine-v21/scripts/data_loader.py` 为同名不同文件，勿混淆
## 14:40 SELL_LADDER 脚本审计 — 持续有效 vs 一次性 (DATA/REINFORCED)

- **触发**: 用户问 "有哪些 backtest/batch 持续有效? 为什么 .py 持续增加?"
- **审计结果** (8 个 .py):
  | 脚本 | 类型 | 持续有效? | 说明 |
  |------|------|-----------|------|
  | sell_ladder.py | 核心引擎 | ✅ 每日用 | 16 skill 内联，唯一分析入口 |
  | data_loader.py | 数据加载 | ✅ 新 | 统一加载 (集中目录→fallback→Sina) |
  | batch_v25_holdings.py | 批量(持仓) | ✅ 18只每日用 | 用 today 动态日期，走 load_data 集中化 |
  | batch_sell_ladder.py | 批量(v2.2) | ✅ 可跑(旧版) | 17持仓，已自动 fallback 到中央数据 |
  | batch_pure_report.py | 报告(纯指标) | ⚠️ 一次性 | 硬编码 runs/2026-08-10/ JSON，历史快照 |
  | backtest_v090.py | 回测(BT-009) | ✅ 复跑用 | v2.5 每周五，与 BT-009-v25 基线一致 |
  | backtest_seed_2026.py | 回测(v2.0种子) | ⚠️ 参考 | BT 方法论原始验证，被 v090 取代 |
  | migrate_data.py | 迁移 | ❌ 一次性 | 迁移已完成，保留为可重跑工具 |
- **关键结论**: 真正"持续有效"的是分析链 (sell_ladder + data_loader + batch_v25_holdings); 回测脚本是"按需复跑"; batch_pure_report/migrate_data 是一次性的
- **关于 .py 增加**: 每次诚实建模都新增独立脚本以保可重跑 (BT-009 系: seed→v090 演化; batch 系: v25 取代 v2.2)，符合 memory-protocol 留痕要求，非冗余堆积
- **状态标签**: REINFORCED

## 16:30 BUY_LADDER v1.1 设计 + 实现 (NEW)

- **触发**: 用户提出"如何设计买入点位的衡量和判断？买入信号和卖出信号如何综合考量？会有冲突吗？" → 与既有 sell-ladder v2.5 形成对偶
- **决策路径**:
  1. **v1.0 草案** (用户初稿)：5 道确认 + 5 道否决 + Layer 1 选股 + 模式 A 突破 / 模式 B 回调
  2. **5-Why 反方质控** (skill `5-why-adversary`): v1.0 触发逻辑"WT1 从负转正 = 反转"被 **BT-007 deep-recovery 65 事件证伪**（120d 中位 -7.82%, 胜率 17%）→ v1.0 应被驳回
  3. **v1.1 修订**：触发逻辑改为 **"WT1 进入甜区 [20, 40]"**（非零穿越）+ 新增 **Layer 0 regime 闸门** (3 重条件必须通过) + 6 个 transition 函数 + sell-buyer 闭环
- **实施** (Phase 0-6):
  1. 读 sell_ladder.py / data_loader.py / batch_v25_holdings.py / backtest_v090.py 确定复用接口
  2. `8.BUY_LADDER.md` 设计文档：3 层架构 + 4 阶段 + 3 模式 + 6 种 buy/sell 冲突仲裁 + 4 个 5-Why Challenge 区块
  3. `.opencode/skills/buy-ladder/SKILL.md` 协议层：仿照 sell-ladder SKILL.md 风格
  4. `buy_ladder.py` 实现 (640 行)：import sell_ladder 16 calc_* + 6 个新增 (calc_wt1_sweet_zone_transition / calc_trend_launch_transition / calc_volume_price_resonance / calc_structure_complete + 2 个占位符 calc_valuation_percentile / calc_fundamental_inflection)
  5. `batch_v11_holdings.py` 批量验证: 18/18 持仓全部正确进入 "禁入区-Layer0锁定"（国家队净卖出 + MCI Q3 + MA60 仅 -2.66%）
  6. `combo_layer.py` v0.1 联合调度：300725 测试 = sell stage 1 + buy stage 4 = 持有（最强动能期不卖，但不追高）
- **关键发现**:
  - **sell_ladder bug**: `calc_alpha_engine_v21` 内部 `_compute_wt(df)` 返回新 df 但不修改原 df（局部变量重绑定）→ buy_ladder 内部必须**显式调用 `sl._compute_wt(df)`** 才能让后续信号看到 wt1/wt2 列
  - **Layer 0 数据缺失**: 沪深 300 ETF (510300) 不在 data/market/daily, wif-ashare-advisory 无 CSV → 引入 CHINA_FRAMEWORK §11 文档记录值兜底（-2.66% / 0.386）
  - **300725 force-unlock 测试**: WT1=+44.07 (已超甜区，等待回调) + 5 确认 2/5 + 0/5 否决 → 阶段 2 观察区（v1.1 设计的"不追高"逻辑生效）
- **Layer 0 当前状态 (2026-08-11)**:
  | 条件 | 当前值 | 解锁? |
  |:---|:---:|:---:|
  | 沪深300 MA60 | -2.66% | ✅ (-2.66 > -7) |
  | WIF MCI | 0.386 | 🔒 (<0.5, Q3 防御) |
  | 国家队 regime | -89% vs 峰值 | 🔒 (净卖出) |
  | **综合** | 2/3 锁定 | **🔒 buy-ladder 关闭** |
- **状态标签**: NEW
- **遗留 (P1/P2)**:
  - [P0] sell_ladder 内部 `_compute_wt` 不回传 bug (待修)
  - [P1] 板块 ETF 覆盖扩展 (akshare 行业查询接口)
  - [P1] Layer 1 估值/基本面数据源接入 (tushare daily_basic + fina_indicator)
  - [P2] BT-010 buy-ladder 回测 (alpha-engine-v21 HDF5 + 自建 buy 信号反演)
  - [P2] 5-Why 阈值参数网格扫描 (ADX 收敛天数 / 回调幅度 [3%/5%/7%/10%] / 量缩阈值)
  - [P2] regime 数据自动刷新机制 (当前用文档值兜底, 应有自动 pipeline)

## 18:00 BUY_LADDER v3.0 — 回滚 percentile + 对齐 LazyBear/Vibe-Trading canonical (REVERTED)

- **触发**: 用户批评"v1.2 percentile 自适应是'和稀泥'，失去理论原本的规则"，要求恢复到经典理论值和公式阈值
- **决策路径**:
  1. **用户指示**："应该使用 LazyBear WT1 公式的 [60/-60] 经典阈值 + vibe-trading 的指标和算法"
  2. **用户指示**："vibe-trading 刚刚更新了代码和版本，请仔细查看并同步"
  3. **用户指示**："sell-ladder 和 buy-ladder 需要同步地、协调地对齐到原本的算法和逻辑"
  4. **用户指示**："积分规则和权重需要仔细思考，让买卖点合乎市场规律并能协调工作"
- **实施**:
  1. **Phase 0**: 探索 Vibe-Trading (`/Users/weimingzhuang/Documents/source_code/Vibe-Trading`) — 找到 `agent/src/skills/{technical-basic, ichimoku, smc, candlestick, volatility, ml-strategy, multi-factor, alpha-zoo, harmonic, pair-trading}` 的 canonical 实现
  2. **Phase 1**: 发现 7 个差异 — **ADX=20 vs 25**, **smc swing=50 vs 10**, **ichimoku 2 重 vs 3 重**, ml-strategy/alpha-zoo 命名误导, V21 adaptive OB filter 缺失
  3. **Phase 2**: 完全回滚 buy_ladder v1.2 的 percentile/quantile_thresholds 函数 (共删除 ~370 行) — 恢复经典 LazyBear 阈值
  4. **Phase 3**: 修正 sell_ladder.py — ADX=20→25, smc swing=50→10, ichimoku 加 bullish_cloud 第 3 重过滤
  5. **Phase 4**: 实现 V21 Adaptive OB Filter (`ob_base=53, ob_cap_adj=40, threshold=53+40*mcap_pctile`) — 跨截面 mcap 自适应
  6. **Phase 5**: buy/sell 协调机制重设计（6 种组合表 + sell 永远优先于 buy）
  7. **Phase 6**: 18 持仓批量验证通过 (18/18 Layer 0 锁定, sell-ladder batch 跑通)
- **关键发现 (Vibe-Trading canonical)**:
  - **technical-basic**: `adx_threshold=25` (非 20), `rsi_oversold=30, rsi_overbought=70`, `bb_window=20, bb_std=2.0`
  - **ichimoku**: `tenkan=9, kijun=26, senkou_b=52, displacement=26`, 信号需 3 重 (tk_cross + above_cloud + bullish_cloud)
  - **smc**: `swing_length=10` (非 50), `close_break=True`, ChoCH 优先 + BOS 补充 + FVG 同向
  - **volatility**: `hv_window=20, lookback=120, low_pct=20, high_pct=80`
  - **V21 OB Filter** (`alpha_engine_common.py`): `ob_base=53, ob_cap_adj=40, threshold=53+40*mcap_pctile` — 跨截面 mcap 自适应
  - **V21 alpha engine** (`score_v30`): `lv(0.15) + wt_mom(0.85)`, cross-sectional `rank(pct=True)`, n_hold=10, max_per_ind=3, liq_filter_mcap=2e9
- **v3.0 与 v1.0/v1.1/v1.2 对比**:
  - v1.0: 反转式触发（被 BT-007 证伪）
  - v1.1: 经典 [20, 40] 静态 + Layer 0 regime + 简单 transition
  - v1.2: percentile 自适应（被用户批评"和稀泥"）
  - **v3.0**: LazyBear [40, 60] sweet zone + V21 OB filter + 经典参数 + canonical 对齐
- **遗留 (P1/P2)**:
  - [P1] buy_ladder batch 模式集成 V21 OB filter（需要 universe mcap 数据）
  - [P1] smc 信号按 Vibe-Trading canonical 重写（ChoCH 优先 + FVG 同向）
  - [P1] ml-strategy / alpha-zoo 函数名澄清（misleading — 不是真 ML/zoo）
  - [P2] BT-010 重设计：用 v3.0 经典阈值 vs V21 OB filter 对比回测
  - [P2] 8.BUY_LADDER.md v3.0 章节完整化（已 append 十四节）
- **状态标签**: REVERTED

## 19:30 BUY_LADDER v3.1 + sell_ladder canonical 精确对齐 (explore 全库审查) (REINFORCED)

- **触发**: 用户要求重新用 explore 审查 Vibe-Trading 最新代码，提取经典算法/阈值/逻辑/指标/信号同步到本项目，并清理仓库无用内容
- **explore 审查发现 (7 个差异)**:
  1. **WT1 `_compute_wt` 结构性偏差**: 用 (H+L)/2 输入价 + N1=10/N2=21；LazyBear 原版是 **hlc3 (H+L+C)/3** + N1=10/N2=21；V21 规范是 close-only + N1=50/N2=105
  2. **ADX 阈值**: 我们 20 vs canonical **25**
  3. **smc swing_length**: 我们 50 vs canonical **10**
  4. **ichimoku 信号**: 我们状态判定 (2重) vs canonical 事件触发 (TK交叉+云上+云带向上 3重)
  5. **candlestick 9 处条件偏差**: engulfing 实体放大要求、piercing/dark_cloud 锚点(前收 vs 前低/前高)、晨星暮星缺跳空、三白兵/三乌鸦开盘条件
  6. **harmonic D 回撤锚点错误**: 我们 |D-X|/XA vs canonical |D-A|/XA + 区间偏差
  7. **volatility 方向相反**: canonical pct<20→+1做多; 我们 pct<30→-1卖出 (有意适配, 已标注)
- **实施修正**:
  1. **`_compute_wt`**: (H+L)/2 → **hlc3 (H+L+C)/3**（对齐 LazyBear 日频原版）
  2. **`calc_candlestick`**: 重写 9 处形态条件（shooting_star 加入 BEAR 列表、engulfing 去实体放大、piercing/dark_cloud 改锚点、晨星暮星加跳空、三白兵/三乌鸦用开盘包含）
  3. **`calc_harmonic`**: D 回撤锚点 X→A + 对齐 Gartley/Bat/Butterfly/Crab 区间
  4. **`calc_ichimoku`**: 状态判定 → **事件触发** (tk_cross_up/down)
  5. **`calc_volatility`**: 标注"卖出场景反向适配"
  6. **`data_loader._sina_download`**: 深市 ETF 前缀修复 (15 开头 → sz, 修复 159915 加载失败)
- **验证**:
  - sell_ladder 300725: WT1=+43.27 (hlc3), candlestick=0 (更严格), ichimoku signal=0 (无交叉事件), smc swings=113 (swing=10)
  - 18 持仓 sell batch: Stage 1=0 / Stage 2=2 (601919, 300725) / Stage 2.5=16 / Stage 3=0
  - 18 持仓 buy batch: 18/18 Layer 0 锁定 ✅
- **清理**:
  - 删除所有 `__pycache__` + `*.pyc` (89 个)
  - 删除 `out/` + `.opencode/out/` 临时目录 (228K)
  - 保留 `sell-ladder/data/mcp/` (MCP 证据链) + runs/ (历史记录)
- **保留的 intentional 差异**:
  - `calc_volatility`: 卖出场景反向适配 (canonical 做多/做空双向 vs 卖出只看空)
  - `calc_alpha_zoo` / `calc_ml_strategy`: 函数名 misleading (非真 zoo/ML), 保留为简化单股动量/短期动量
  - `calc_turnover_anomaly` / `calc_ad_line` / `calc_sector_relative`: ladder 独有, Vibe-Trading 无对应 skill
- **状态标签**: REINFORCED
- **遗留 (P1/P2)**:
  - [P1] smc 信号按 canonical 重写 (ChoCH 优先 + FVG 同 bar 过滤)
  - [P1] ml-strategy 实现真 sklearn walk-forward 或重命名
  - [P1] multi_factor 对齐 canonical 四因子 z 等权和
  - [P2] BT-010 重设计: v3.1 经典阈值 vs V21 OB filter 对比

## 20:30 P1 四项完成 — smc/ml/multi_factor canonical + BT-010 首跑 (NEW)

- **触发**: 用户要求继续实施 4 项 P1 待办
- **实施 1: smc canonical 重写** (sell_ladder.py calc_smc)
  - **修复 bug**: 原实现把 bos_choch DataFrame 当 Series 用 (dropna 全删 → 永远"无结构信号")
  - **对齐 canonical**: structure = ChoCH 优先 + BOS 补充; buy = struct==1 & FVG>=0; sell = struct==-1 & FVG<=0
  - swing_length=10, close_break=True
  - 验证: 300725 swings=113 (原 27), 茅台 900 bars BOS 16 次/CHOCH 5 次 (低频事件正常)
- **实施 2: ml-strategy 真 sklearn walk-forward** (sell_ladder.py calc_ml_strategy)
  - 实现 canonical 10 特征 + RandomForest(100, depth5) + walk-forward + predict_proba→[−1,1]
  - 数据≥252 用真 ML; 不足回退到 5d 动量 (try/except 防崩溃)
  - 验证: 300725 训练 2097 样本, prob_up=0.43 → signal=-1 (5d 涨 38% 但 ML 预测下跌)
  - 耗时 1.57s/标的, 18 标的 batch 6.8s
  - ⚠️ BT-002 已证单股 ML 无效, 此信号仅 16 信号中 1 票, 不作为独立依据
- **实施 3: multi_factor canonical 四因子 z 等权和** (sell_ladder.py calc_multi_factor)
  - 从自定义 (f1+f5-|f2|-f3)/4 → canonical 4 因子 (momentum, -reversal, -volatility, volume_ratio) 时间序列 z 等权和
  - 300725 composite=-1.47 (reversal z=-5.67 拖累, 涨多短期反转看空)
- **实施 4: BT-010 对比回测首跑** (backtests/BT-010/script.py)
  - 用 alpha-engine-v21 HDF5 月度数据, 对比 C1 经典[40,60] / C2 V21 OB / C3 BH
  - 结果: C2 NAV=13.463 (+1246%) >> C1 9.515 (+851%) > C3 5.629 (+463%)
  - **V21 OB filter 显著有效**: 过滤 216 次假信号, 收益反而更高
  - 简化版 (仅 WT1 甜区单信号, 未含完整 Layer 0/1/2), Adversarial Review 已记录
- **额外修复**: data_loader._sina_download 深市 ETF 前缀 (15→sz), 修复 159915 加载失败 (600050 batch 之前报错)
- **状态标签**: NEW
- **遗留**: P2 — BT-010 加交易成本重跑; Layer 1 数据接入; 分 regime 统计; smc 按 canonical 单 bar (现为近10bar聚合)

## 21:30 真实批量验证 — sell v2.5 + buy v1.1(代码v3.0) 18 持仓 (NEW)

- **触发**: 用户要求用新版本 sell/buy ladder 重新分析 18 只持仓，验证 (1) .py 包稳定 (2) 框架与 vibe-trading 算法逻辑一致 (3) 与旧版本结果对比合理性 + 市场吻合度
- **执行**:
  - SELL_LADDER v2.5 批量 (`batch_v25_holdings.py`): 18/18 无崩溃 → Stage 1=0 / Stage 2=1 (601919, 6/14) / Stage 2.5=17 / Stage 3=0
  - BUY_LADDER 批量 (`batch_v11_holdings.py`): 18/18 Layer 0 锁定 (n_locked=18, n_error=0), 0.0s/只 (短路设计)
  - force-unlock 601919 端到端: Layer0 解锁 → Layer1 5/5 → Layer2 16 信号+6 转换 → Stage 4 (4/20=20%) ✅
  - CLI 单标的 `sell_ladder.py --ticker 601919 --no-cdmo`: 端到端通过; score 5/14 (vs 批量 6/14, 因 --no-cdmo 无板块数据, 同一 score_v22)
- **框架一致性确认**:
  - `buy_ladder.py:37 import sell_ladder as sl` 零修改复用 calc_* / score_v22 / stage_v22 ✅
  - canonical 参数本次运行全部生效 (hlc3 WT1, candlestick 9 修正, ML walk-forward, smc ChoCH, multi_factor 4 因子 z) ✅
- **新旧对比 (08-10 vs 08-11)**: 5/18 变化 — 300725 ↓2 档 (candlestick+ml_strategy 翻转), 300142 ↓1 档, 601669 ↑2 档, 601688 ↑1 档 (后两者 = smc 假信号消失), 601919 稳定; 全部归因于 20:30 P1 canonical 对齐, 非随机漂移
- **⚠️ 发现 5 个问题 (详见报告 §5)**:
  1. CLI 显示标签过期 (v2.4 判定 + "/9 正 含 ad_line", 实际 v2.5 用 8 票) — sell_ladder.py:1174-1175
  2. 版本号漂移: buy_ladder 代码头 v3.0 vs README/batch v1.1; buy 输出 header 打印 v1.1 (buy_ladder.py:853)
  3. [MEDIUM] BUY Layer 0 MCI/国家队/MA60 为 CHINA_FRAMEWORK 文档 fallback, 非实时数据 (P1 遗留项)
  4. buy 结果 JSON 按 datetime.now() 落 runs/2026-08-10/ 目录 (数据最后日期 vs 运行日期混淆)
  5. Layer 1 market_cap=None 占位通过筛子 (README 已标注待接入)
- **市场吻合度**: 唯一 Stage 2=601919 强动量 (+10.8% 20d) ✅; 降档标的有弱市/信号翻转支撑 ✅; 张力点 300725 (月+30% 但 Stage 2.5, end_count=0 → "减 40% 观察" 非清仓)
- **5-Why 摘要**: 核心评分一致性经得起检查; 最薄弱 = Layer 0 fallback 数据, "18/18 锁定"可能在数据接入后反转 → 结论降级为 "核心一致 + 5 项已知漂移"
- **报告**: `.opencode/out/verify_ladder/2026-08-11/verify_sell_buy_ladder_v25_v11.md`
- **遗留**:
  - [P1] 版本标签统一 (buy_ladder v1.1→v3.0: 代码头/README/batch/SKILL.md)
  - [P1] CLI 显示标签修正 (v2.4→v2.5, /9→8 票)
  - [P2] Layer 0 实时数据刷新 (MCI/国家队/MA60)
  - [P2] 300725 单独复核 (ML −1 单票对高动量股是否过度惩罚)
- **状态标签**: NEW
