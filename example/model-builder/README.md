# Model Builder Cookbook

**Sample questions** for the `model-builder` subagent. Wealth-Guide routes questions about Build DCF/LBO/3-statement models from scratch here.

**Data**: Sample CSV / JSON inputs are in `data/`.

**Routed by**: `wealth-guide` (single-entry agent) → `model-builder` (subagent)
