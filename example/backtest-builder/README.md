# Backtest Builder Cookbook

**Sample questions** for the backtest-builder subagent. Covers strategy development and backtesting across 11 markets.

**Data**: Optionally place CSV/Parquet market data in `data/` for local loader use.
